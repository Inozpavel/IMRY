<Window x:Class="WorkReportCreator.ReportsPage"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WorkReportCreator"
        mc:Ignorable="d"
        Title="Report" Height="450" Width="800" WindowStartupLocation="CenterScreen">
    <TabControl x:Name="Tabs" Background="#FF1E1E1E"> 
        <TabItem Header="Быстрые действия">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <StackPanel.Resources>
                    <Style TargetType="Button">
                        <Setter Property="HorizontalAlignment" Value="Center"/>
                        <Setter Property="VerticalAlignment" Value="Center"/>   
                        <Setter Property="Margin" Value="5"/>
                        <Setter Property="Padding" Value="10"/>
                        <Setter Property="FontSize" Value="14"/>
                        <Setter Property="Width" Value="200"/>
                    </Style>
                </StackPanel.Resources>
                <Button Content="Сохранить все отчеты"/>
                <Button Content="Вернуться назад" Click="ShowWorkAndStudentInformation"/>
            </StackPanel>
        </TabItem>
    </TabControl>
</Window>
